
import TableStatusColumn from './src/StatusColumn'

TableStatusColumn.install = function install(Vue) {
  Vue.component(TableStatusColumn.name, TableStatusColumn)
}

export default TableStatusColumn
